# 用法说明

## 适用时机

- 问题太宽，无法直接调查。
- 用户需要工作计划，而不只是观点。
- 多个解释或方案需要共享结构。

## 误用风险

- 做出整齐议题树，但并不改变决策。
- 把问题硬塞进熟悉框架。
- 收集事实，却不写 so-what 含义。

## 边界

- 起点论证很弱时，先用 `cogm-critical-thinking`。
- 既有分类很可能是继承假设时，使用 `cogm-first-principles`。
- 输出必须转成领域策略时，交给 `cogt-product` 或 `cogt-lead`。
